# requirements.txt
# Python 3.10+ required (spec §0)
# Production-only dependencies for KubeCost-Gym RL Environment
# (dev/test packages removed — container must run on vcpu=2, memory=8gb)

pydantic==2.12.5
fastapi==0.135.3
uvicorn==0.42.0
openai==2.30.0
openenv-core[core]==0.2.2
PyYAML==6.0.3
gradio==6.11.0
