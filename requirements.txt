# requirements.txt
# Python 3.10+ required (spec §0)
# Production-only dependencies for KubeCost-Gym RL Environment
# (dev/test packages removed — container must run on vcpu=2, memory=8gb)

# Core environment & validation
pydantic>=2.0.0

# HTTP API server (OpenEnv REST interface — serves /reset, /step, /state)
fastapi>=0.100.0
uvicorn>=0.23.0

# OpenAI Client (spec requirement — all LLM calls must use OpenAI client)
openai>=1.3.0

# YAML parsing (openenv.yaml)
PyYAML>=6.0
